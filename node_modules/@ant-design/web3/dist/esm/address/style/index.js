import { useStyle as useAntdStyle } from "../../theme/useStyle";
const genAddressStyle = token => {
  return {
    [token.componentCls]: {
      wordBreak: 'break-all'
    }
  };
};
export function useStyle(prefixCls) {
  return useAntdStyle('Address', token => {
    const proListToken = {
      ...token,
      componentCls: `.${prefixCls}`
    };
    return [genAddressStyle(proListToken)];
  });
}