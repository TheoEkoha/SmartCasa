import React from 'react';
import { type Token } from '@ant-design/web3-common';
import type { SelectProps } from 'antd';
export interface TokenSelectProps extends Omit<SelectProps, 'value' | 'onChange' | 'options' | 'mode'> {
    /**
     * selected value
     */
    value?: Token | Token[];
    /**
     * selected value change handler
     */
    onChange?: (value: Token) => void;
    /**
     * @deprecated
     * controlled token list
     * please use options instead
     */
    tokenList?: Token[];
    /**
     * token list
     */
    options?: Token[];
    /**
     * select mode
     */
    mode?: 'multiple';
}
export declare const TokenSelect: React.FC<TokenSelectProps>;
