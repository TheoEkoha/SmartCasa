import { useContext } from 'react';
import { LeftOutlined } from '@ant-design/icons';
import { connectModalContext } from "../context";
import { jsx as _jsx } from "react/jsx-runtime";
import { jsxs as _jsxs } from "react/jsx-runtime";
const MainPanelHeader = props => {
  const {
    back = true,
    title,
    onBack
  } = props;
  const {
    prefixCls,
    panelRouteBack,
    canBack
  } = useContext(connectModalContext);
  const handleBack = async () => {
    if (!onBack) {
      panelRouteBack();
      return;
    }
    const isBack = await onBack();
    if (isBack !== false) {
      panelRouteBack();
    }
  };
  return /*#__PURE__*/_jsxs("div", {
    className: `${prefixCls}-main-panel-header`,
    children: [back && canBack && /*#__PURE__*/_jsx("div", {
      className: `${prefixCls}-main-panel-header-back`,
      onClick: handleBack,
      children: /*#__PURE__*/_jsx(LeftOutlined, {})
    }), title && /*#__PURE__*/_jsx("div", {
      className: `${prefixCls}-main-panel-header-title`,
      children: title
    })]
  });
};
export default MainPanelHeader;