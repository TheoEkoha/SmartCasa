import { TinyColor } from '@ctrl/tinycolor';
export const mergeReactNodeProps = (node, defaultNode, resolveString) => {
  if (node === null || node === false) return node;
  if (typeof node === 'string' && resolveString) return resolveString(node);
  return node ?? defaultNode;
};
export const defaultGroupOrder = (a, b) => {
  if (a === 'Popular' && b !== 'Popular') {
    return -1;
  }
  if (a === 'More' && b !== 'More') {
    return 1;
  }
  return a.localeCompare(b);
};
export const isDarkTheme = token => {
  const hsv = new TinyColor(token.colorFill).onBackground(token.colorBgElevated).toHsv();
  return hsv.v < 0.5;
};