import { Grid } from 'antd';
export default function useMode(mode = 'auto') {
  const {
    md
  } = Grid.useBreakpoint();
  const isSimple = mode === 'simple' || mode === 'auto' && !md;
  return {
    isSimple,
    md
  };
}