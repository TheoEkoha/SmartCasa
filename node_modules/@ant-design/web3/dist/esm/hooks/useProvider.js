import React from 'react';
import { ConfigContext } from '@ant-design/web3-common';
function mergeObjects(a, b) {
  const result = {
    ...a
  };
  if (b) {
    for (const key in b) {
      if (b && b[key] !== undefined) {
        result[key] = b[key];
      }
    }
  }
  return result;
}
export default function useProvider(props) {
  const context = React.useContext(ConfigContext);
  return mergeObjects(context, props);
}