import {
  keccak256,
  stringToBytes,
  toBytes
} from "./chunk-CY3CMUVN.js";
import {
  concat
} from "./chunk-4WCZ4M4G.js";

// node_modules/thirdweb/dist/esm/utils/hashing/hashMessage.js
var presignMessagePrefix = "Ethereum Signed Message:\n";
function hashMessage(message, to_) {
  const messageBytes = (() => {
    if (typeof message === "string") {
      return stringToBytes(message);
    }
    if (message.raw instanceof Uint8Array) {
      return message.raw;
    }
    return toBytes(message.raw);
  })();
  const prefixBytes = stringToBytes(`${presignMessagePrefix}${messageBytes.length}`);
  return keccak256(concat([prefixBytes, messageBytes]), to_);
}

export {
  hashMessage
};
//# sourceMappingURL=chunk-OJSKMF74.js.map
